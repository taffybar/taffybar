# Changelog for gtk-scaling-image

## 0.1.0.0

- Initial release.
- Extract generic pixbuf scaling, CSS inset measurement, and draw-based
  auto-fill image helpers from `taffybar` and `gtk-sni-tray`.
