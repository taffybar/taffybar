# Direnv environment for this repo.
# Requires nix-direnv: https://github.com/nix-community/nix-direnv

watch_file flake.nix
watch_file flake.lock
watch_file package.yaml
watch_file gtk-sni-tray.cabal

use flake
